meta {
  name: Admin - Toggle Template
  type: http
  seq: 14
}

put {
  url: {{baseUrl}}/bouquets/42/toggle-template
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "is_template": true
  }
}
